# HubSpot Private App access token.
# Create a Private App in each portal and grant the scopes required for the
# objects you want to manage (crm.objects.contacts.read/write,
# crm.schemas.contacts.read/write, etc.).
HUBSPOT_ACCESS_TOKEN=pat-na1-...

# Optional: default environment label used in the manifest.
HUBSPOT_ENV=default
